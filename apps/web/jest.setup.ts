// Testing Library's DOM matchers (`toBeInTheDocument`, `toHaveFocus`, …) for
// every web spec. Nothing else belongs here yet — per-spec setup stays in the
// spec, so a reader never has to hunt for hidden global state.
import "@testing-library/jest-dom";
