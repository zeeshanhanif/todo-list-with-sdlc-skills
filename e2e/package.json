{
  "name": "@todo/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "System-level end-to-end suite (Playwright). Seeded by the walking-skeleton test; extended per architecture-named critical flows.",
  "scripts": {
    "test": "playwright test",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
